{
  "name": "@templar/assets",
  "version": "0.0.1",
  "private": true,
  "description": "Shared brand and default app assets for Templar Labs.",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed for shared assets.'",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "lint": "echo 'Linting is handled by the root lint pipeline.'",
    "test": "echo 'No tests configured yet.'",
    "typecheck": "echo 'No typecheck needed for asset-only package.'"
  },
  "exports": {
    "./favicon.svg": "./src/favicon.svg"
  },
  "files": [
    "src"
  ]
}
