/**
 * Root domains allowed to receive production first-party auth callbacks.
 *
 * Each entry allows both the root domain and all of its subdomains.
 */
export const firstPartyAllowedRootDomains = ["ericventor.com"] as const;
