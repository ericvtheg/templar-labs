{
  "name": "@templar/deploy",
  "version": "0.0.1",
  "private": true,
  "description": "Shared deployment conventions for Templar Labs projects.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "lint": "echo 'Linting is handled by the root lint pipeline.'",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@templar/config": "workspace:*",
    "alchemy": "^0.93.7"
  },
  "devDependencies": {
    "@types/node": "^24.12.4"
  },
  "exports": {
    ".": "./src/index.ts",
    "./bindings": "./src/templar-bindings.ts",
    "./cloudflare": "./src/cloudflare/index.ts"
  }
}
