{
  "name": "@templar/eslint-config",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build needed for shared eslint config.'",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "lint": "echo 'Linting is handled by the root lint pipeline.'",
    "test": "echo 'No tests configured yet.'",
    "typecheck": "echo 'No typecheck needed for shared eslint config.'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
