{
  "name": "@templar/scheduler",
  "version": "0.0.1",
  "private": true,
  "description": "Recurring cron task conventions",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "lint": "echo 'Linting is handled by the root lint pipeline.'",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@templar/tsconfig": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^24.12.4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
