{
  "name": "@templar/dev-ports",
  "version": "0.0.1",
  "private": true,
  "description": "Manage local development port assignments.",
  "license": "ISC",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "dev-ports": "node src/index.ts",
    "lint": "echo 'Linting is handled by the root lint pipeline.'",
    "test": "node --test src/index.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@templar/tsconfig": "workspace:*",
    "@types/node": "^24.12.4",
    "typescript": "6.0.3"
  }
}
