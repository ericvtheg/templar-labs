{
  "name": "@templar/mobile-ci",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "lint": "biome check .",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@templar/tsconfig": "workspace:*",
    "@types/node": "^24.12.4",
    "typescript": "6.0.3"
  }
}
